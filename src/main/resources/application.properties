# Enabling H2 Console
# spring.h2.console.enabled=true

# Allow running through docker container
# spring.h2.console.settings.web-allow-others=true

# Custom H2 Console URL
# spring.h2.console.path=/h2

# Custom Banner txt
spring.banner.location=static/banner/banner.txt

## Custom Banner img
#spring.banner.image.location=static/banner/banner.png

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_PORT_3306_TCP_ADDR:localhost}:${MYSQL_PORT_3306_TCP_PORT:3306}/${MYSQL_ENV_MYSQL_DATABASE:meeting_app_db}
spring.datasource.username=${MYSQL_ENV_MYSQL_USER:danielpm1982_user}
spring.datasource.password=${MYSQL_ENV_MYSQL_PASSWORD:admin}
spring.datasource.driver-class-name =com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.show-sql: true
